# CUDA 12.6 is the last PyTorch wheel line that retains Volta/V100 (sm_70)
# kernels. Keep the local-version suffix so pip cannot select a CUDA 13 wheel.
--extra-index-url https://download.pytorch.org/whl/cu126

numpy>=1.24,<2
pandas>=2.0
PyYAML>=6.0
scipy>=1.10
scikit-learn>=1.3
torch==2.13.0+cu126
